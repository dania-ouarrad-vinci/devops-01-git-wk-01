import { Main } from "../src/app.js";

describe("the first minute line", function () {
  let main = new Main();

  it("function should return ", function () {
    let result = main.BerlinClock();
    expect(result).toBe();
  });
});

// allumée ou éteinte
